# Session 01 · 2026-08-17 · Bootstrap, Welt, Spieler (M0.1–M0.3)

## Ziel
Vendoring, Bootstrap-Loop mit Rapier, Weltausschnitt (Terrain, Wald, Himmel), Spieler am Boden –
integriert und committet; Plan um Mockup 1:1 und Vor-Ort-Fotos ergänzt.

## Start-Commit
`8eff81c docs(research): on-site photo findings …`

## Erledigt
- Fotos nach `docs/reference/photos/` kopiert; Mockup-Spezifikation `docs/reference/mockup/README.md`;
  ADR-025/026/027 (Mockup 1:1, UI EN+DE, Kategorien mit Green); PROMPT/GDD/ROADMAP angepasst.
- `vendor/`: three 0.185.1 (`three.module.js` + `three.core.js`), rapier3d-compat 0.20.0
  (`rapier.mjs`, WASM inline) – `tools/vendor.ps1`.
- M0.1: `index.html` (Import-Map, Favicon), `css/{base,hud,screens}.css`, `js/config.js`,
  `core/{loop,input,rng,params,errors,events,renderer,physics}.js`, `ui/debug.js`, `tests/unit/rng.test.mjs`.
- M0.2/M0.3: `world/*`, `procgen/*`, `player/*`, `tools/dev/*.html` (Terrain, Wald, Himmel, Spieler),
  Integration in `main.js`; F2/`?physics=1` als eigener Wireframe-Toggle.
- Regel „Subagenten nie auf Fable“ in CLAUDE.md/PROMPT.md/AGENTS.md; `.gitattributes` (LF).

## Dateien neu
`git show --stat d39dc12..HEAD`

## Dateien geändert
`js/main.js` (Neuaufbau), `js/core/{input,params,physics}.js` (F2), Docs.

## Entscheidungen (→ ADR)
ADR-025, 026, 027. Faktisch: Three.js 0.185.1, Rapier compat 0.20.0 (als ADR-013/014 eintragen).

## Probleme / Fallen
- **Ein Subagent lief ohne `model`-Angabe auf Fable 5 und hat ~240k Tokens verbrannt** – Regel jetzt fest.
- Rapier-Debug-Wireframe des Heightfields überdeckt die Szene → eigener Toggle.
- `node --test <dir>` funktioniert in Node 22 nicht – Glob nötig.
- Headless-fps (SwiftShader) sind konservativ; Draw-Calls/Dreiecke sind die belastbaren Zahlen.

## Tests durchgeführt
`node tools/check-all.mjs` 47/47 · `npm test` 13/13 · Browser: `?debug=1` lädt, 0 Fehler/Warnungen,
0 externe Requests, WASD-Bewegung, `grounded=true`, Screenshot `docs/screenshots/m0-2-world-first-integration.png`.

## Performance-Beobachtungen
220 Draw-Calls, 1,67 M Dreiecke (Terrain ~1,3 M → Chunk-LOD nötig), ~77 fps headless, Physik < 1 ms.

## Screenshots
`docs/screenshots/m0-1-bootstrap.png`, `m0-2-world-first-integration.png`, `dev-forest*.png`,
`dev-terrain*.png`, `dev-sky*.png`, `dev-player*.png`.

## Commits
`6b45172` docs mockup+photos · `d39dc12` vendor · `f914042` core boot · `bc9cec7` world integration ·
Docs-Commit danach.

## Offen geblieben
M0.4–M0.7 (siehe `ROADMAP.md`), Terrain-LOD, Laub-Texturmaßstab.

## Empfehlung für die nächste Session
M0.4: Vertrag ergänzen (`docs/architecture.md`), Einstiegsdeck + Blockleiter + Podest + Belay-Automat +
Karabiner-Widget + Klicks bauen, in `main.js` an `heroTrees[0]` hängen, Screenshot, Commit.
